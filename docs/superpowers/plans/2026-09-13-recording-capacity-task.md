# Task 1 — 本地录音按真实容量保护

用户已确认“按容量管理，保留安全余量”，不重新发起产品方案确认。

## 约束与实现范围

工作目录 `E:/REPOSITORY/haisha/haisha/.worktrees/all-books-device-recording`。已有多项未提交分享修复，必须保留，不提交、不还原、不更改项目配置和云函数。

修改 `src/features/listeningPractice/pendingCheckInStore.ts`、`pendingCheckInRuntime.ts`、`src/pages/Practice/Practice.tsx`；必要时新增一个聚焦容量的模块和测试脚本；允许更新依赖这些接口的测试适配器。不要改变页面布局或分享流程。

1. 现有预算 100 MiB 保留。增加固定安全余量 10 MiB；新持久录音不可侵占该余量（录音最多使用 90 MiB）。开麦前额外预留一条文件上限 8 MiB，因此在已有录音占用超过 82 MiB 时拒绝开启新录音，明确提示先整理历史录音。8 MiB 是上传文件上限，不是录音的码率估算。
2. 取消 500 条业务硬限制，空间足够时 501 条短录音也能保存。索引空间的原生限制仍通过原有写失败保护收口，不承诺无限条数。
3. 通过微信文件系统只读统计实际文件使用量，至少包括本应用 saveFile 保存的文件（包含未被索引引用但仍占空间者）。本地录音路径实际大小用于校准容量，不能只按旧索引大小、不能读取每个文件正文或计算全库摘要。若使用用户目录递归 stat，明确处理目录/文件和缓存路径去重。不得把未知统计结果当作 0，当前应用仅在 pendingCheckInRuntime 使用 saveFile 持久保存文件。
4. 录音前检查、保存前检查、保存后实际大小校准均不删除旧录音。原生存储满/元数据写失败仍需保留已经保存的文件路径供恢复。仅检查不是空间预留的原子保证，应在报告说明极端设备空间变化仍可能失败。
5. 开麦前异步检查后，继续调用已有 canStartRecordingNow(request) 复核页面、教材、会话状态。容量不足或检查失败不清空之前那段录音；隐藏页不能补开麦。保存重试路径不能把已保存文件重复计数，也不能丢掉临时/已移动路径。
6. 接口不存在或检查报错应给可理解的短提示并安全拦截新录音；保留既有本地回听与重试恢复能力。诊断仅 console 输出分类错误，不记录录音路径、口令或原始错误全文。
7. 正常界面不新增复杂空间管理面板；只在不足/未知时提示。

## TDD 与验证

先补测试并看到预期失败，然后最小实现。测试包含：空间恰在边界和多 1 字节；超过 500 条但容量足够；未索引实际文件占用；统计失败/无效大小；已保存路径重试不重复计算；保存后大小增大保留恢复路径；检查未结束就离页/换教材不得开麦；不足时旧录音保留。

运行聚焦命令：
```
node scripts/test-pending-check-in.cjs
node scripts/test-pending-check-in-runtime.cjs
node scripts/test-practice-book-route.cjs
node scripts/test-local-recording-retry-runtime.cjs
```
以及所有新增/修改测试。不要改弱已存在的断言。报告 RED/GREEN 证据、接口、改动文件、未验证原生边界至 `.superpowers/sdd/2026-09-13-capacity-report.md`，简短返回状态。不提交，由控制者整体验证当前脏工作区。
