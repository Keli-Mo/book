# 教材页直接回首页导航

用户已确认：沿用回听页顶部布局，左上角“返回箭头＋房子图标”，无底色、无阴影，正文不变；验证离页停止播放和录音保存。

- 教材训练页改用自定义导航，标题“听力跟读训练”。正常教材与无效链接状态都保留导航。
- 复用现有 CheckInNavigation，仅增加可选标题属性，默认“跟读打卡”，避免两份导航的尺寸和安全区处理分叉。
- 房子按钮直接 reLaunch 到 `/pages/Home/Home`。返回箭头在有页面栈时 navigateBack，无上一页时回首页兜底。
- 导航放在全宽页面外壳，正文继续使用现有留白和限宽；不把导航包进正文的内边距。保持既有 44px 命中区、胶囊预留、状态栏和横屏安全区。
- 保留录音、播放和离页收尾逻辑；用实际点击加 hide/unload 生命周期测试确认录音终止回调仍保存本地文件，不新增上传或删除操作。
- 验证正常/错误入口、导航路径、活动与暂停录音离页、已录音播放离页、手机/Pad 横竖屏布局。Edge 受控测试不宣称为真机验收。
- 保留两个用户项目配置文件改动，不推送、部署或清缓存。
