# 分享标题与两项验收阻塞处理

日期：2026-09-18。源码起点`005df0c`，本轮代码提交至`4ec2e7e`。工作区`.worktrees/all-books-device-recording`。

## 已完成的源码修复

1. `2c18d25 fix: 精简分享页跟读完成标题`：分享详情共用标题精确改为“完成英语跟读”，不改其他文案、样式或行为。真实页面运行时断言先RED后GREEN；音频回归通过。独立复审Spec compliant / Approved，无遗留问题。
2. `bde7753 fix: 为过期清理区分准入与收尾预算`：按用户明确批准的实际时限＋安全余量方案，保留1200ms新工作准入，已准入候选可在可信时限减1000ms内继续；时限最多20000ms，非法或缺失按最后核实的3000ms回退。所有事务重试和后续操作发起前检查截止。归属、路径、引用、期限、事务重读、CAS、永久墓碑保持。零进展预算退出输出固定脱敏日志。独立复审通过，无Critical/Important/Minor。
3. `4ec2e7e test: 增加隔离执行的完整回归入口`：新增`npm run test:regression`和[使用说明](regression-command.md)，不新增依赖、不改业务测试断言。主控另以固定Node直接读取实际package.json，确认命令字符串契约通过。

清理真实RED中，旧实现四轮仍留两份文件；新43组测试包含原31组及12组运行时限/慢候选/中断重试边界。原900ms引用查询＋350ms事务＋50ms删除的复现，在新实现多轮能清完两条并回绕。模拟不是实际云端性能证明；保留1000ms只是停止发起后续操作的策略，无法取消已发出的RPC，不保证任意延迟均有进展。

本轮最终独立复审已完成：`005df0c..4ec2e7e`本地代码可合并，无Critical/Important；标准入口Spec compliant / Quality Approved。旧清理复现和全回归证据缺口已关闭，Windows暂停来源仍未知。两项既有布局测试Minor及云端/真机验收继续保留。

## 本轮验证

| 检查 | 实际结果 |
| --- | --- |
| 清理聚焦 | 43/43；包含RED/GREEN与三次冲突耗尽边界 |
| checkIn协议 | 51/51 |
| 本地分享生命周期 | 通过 |
| 源码类型检查 | 固定Node，`tsc --noEmit --skipLibCheck`退出0 |
| 新微信构建 | 进程级`ESBUILD_WORKER_THREADS=0`，退出0；既有Browserslist/Sass/体积warnings保留 |
| 新产物页面布局 | `audit-ui-layout.cjs release-blockers-20260918`，168/168，failed=[] |
| 标题视觉 | 主控亲阅390×844手机及768×1024Pad截图，标题为新文案，无重叠；不是原生真机验证 |
| 一次自制运行器全回归 | **40/41，未通过**，零重试，原每脚本30000ms限制未改 |
| 一次官方运行器独立对照 | **41/41，退出0**，无失败/取消/跳过/todo，16362.4932ms；逐文件进程隔离、并发1、30000ms、零重试 |

使用固定Node：`C:/Users/23237/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe`。

## 完整回归：捕获了实际系统线程暂停

此前三轮40/41失败证据继续保留。本轮没有改业务断言或放宽超时，增加独立文件阶段记录及只读OS观察，执行一次完整41脚本。失败为`test-book-practice.cjs`，30016ms；此前失败的返回、协调器、UI脚本本轮通过。清理43/43亦通过。

本机证据目录：`.superpowers/sdd/2026-09-18-timeout-observed-2026-09-18T00-41-59-637Z-94324/`，含`run.json`、`regression.json`、`observer.jsonl`及逐项阶段记录。不会以新报告覆盖旧失败。

失败子进程PID95168，在第5秒和第15秒均通过父子PID、可执行文件、创建时间、入口脚本及preload身份核对。两次CPU均203.125ms、工作集均69795840字节；13个线程中12个为Wait/Suspended，另一个Wait/Unknown。系统空闲物理内存约16GB。最后文件阶段为TypeScript加载完成（106.8545ms），随后没有测试完成或退出记录。

这支持**系统层线程暂停**，反证该10秒观察区间内持续JavaScript死循环或单纯慢编译；但还不证明谁发起暂停，也不能排除原生异常报告、调试器或其他系统组件。不能直接归因杀毒软件，不能仅因目标测试单独通过就宣称修复。

OS观察器在线读取阶段文件发生共享冲突，已在两次快照中保留错误；进程结束后成功读回完整文件阶段记录。CPU/线程身份快照不依赖阶段文件读取。观察器已自然结束，未人工暂停/恢复/终止任何无关进程。

限定65秒事件窗口内，没有支持该PID崩溃、资源耗尽或代码完整性故障的记录；Defender仅有健康报告，不能据此归因。选定调试环境变量不存在；已知位置没有Node WER或转储文件。暂停来源仍未确定，不擅自修改系统安全策略或恢复被暂停进程。

## 标准运行器对照与常规回归入口

现有Docker引擎未启动，WSL仅有停止的docker-desktop；未擅自启动它们。改用本机现成官方Node运行器做**一次独立调度路径对照**，不是修改业务代码或重试失败项。官方说明普通可执行脚本无需内部使用node:test，默认按子进程退出码判定，支持逐文件隔离：[Node v24执行模型](https://nodejs.org/docs/latest-v24.x/api/test.html#test-runner-execution-model)。本机`--help`也确认所用选项存在。

```powershell
node --test --test-concurrency=1 --test-timeout=30000 "scripts/test-*.cjs"
```

实际执行使用上述固定Node，额外配置spec终端和TAP文件双报告。结果41/41通过，零失败/取消/跳过/todo，耗时16362.4932ms；TAP保存在`.superpowers/sdd/2026-09-18-standard-runner.tap`。主控将TAP中的41个通过文件名与原观察运行的完整清单逐项比对，Difference=[]，没有漏测。输出有既有Browserslist数据库过旧提醒，不称输出完全无警告。

这为当前源码提供了完整回归通过证据，常规入口使用同一官方命令；**不证明旧Windows暂停根因已经消除或以后不会再出现**。自制运行器与诊断preload构成一组调度条件变化，不能仅凭这组对照断言execFileSync本身有缺陷。若标准入口再出现暂停，继续按已捕获OS边界做定向诊断，不用增加超时或重复到绿掩盖。

## 部署与发布边界

此前复审的两项布局测试改进仍保留：大字号需进一步检查相邻文字实际绘制区域交叠；远程图片的五秒等待可改为本地固定测试资源，并另外验证远程可达性。本轮168项检查未出现布局失败，但这些自动化覆盖不足不能据此取消真机验收。

本轮三个代码与测试提交仅在本地，尚未更新云函数或上传小程序体验版；没有开启生产自动清理、改套餐、改权限、删除真实录音或推送Git。两份IDE配置摘要与本轮起点相同，主目录旧改动未碰。

整体上线仍需此前列出的真实云权限、运行配置、两账号和iOS/Android/iPad录音验收。不要将本报告中的本地通过当成完整上线验收。
