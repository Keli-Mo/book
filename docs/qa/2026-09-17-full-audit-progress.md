# 全面核查与完善：阶段证据

状态：**进行中，不能据此发布正式版或启用生产自动删除。**

后续更新：2026-09-18已修复慢单条清理预算，并取得官方运行器完整41/41回归通过证据；下文保留当时失败记录。当前处理结果与仍待验收项目见[后续报告](2026-09-18-release-blockers.md)，不将新通过回写成旧失败不存在。

工作树 `.worktrees/all-books-device-recording`，分支 `codex/practice-home-navigation`；本轮基线 `abc5666`。主目录旧改动和两份IDE配置未纳入。

## 已验证

- 用户更新的两云函数已在2026-09-16通过官方CLI重新下载：共6个入口/清单/配置文件与基线一致。两函数均Active、Nodejs16.13、超时3秒。源码config的空triggers不证明平台没有定时器。
- 基线自动回归40/40，无自动重试；TypeScript `--noEmit --skipLibCheck`、微信构建通过。构建保留第三方Sass、Browserslist及webpack体积建议，不能把webpack建议当微信分包限制。类型检查命令跳过依赖声明检查，不能称为所有依赖类型均已通过。
- URL可达性6101/6101（4071图片、2030唯一音频）：并发4、小字节Range、零重试。不是逐段听音、内容准确性或素材授权验收。
- 基础DOM布局160/160；补充23册首/中/末真实训练×手机/Pad视口138/138，无图片加载失败或水平溢出。另做20次文字压力检查，定位到首页和我的录音大字号标题截断，现已由下述修复处理。上述不是微信真机/系统字体/录音权限验收。
- 错误脱敏：`12efdcf`、`166f3d1`，独立复审通过。checkIn38/38、cleanup14/14、客户端14/14及错误分类/本地分享集成/tsc通过。上下文读取异常、null事件和未知action也进入固定分类。
- 清理预算与检查点：`6c64a1b`，独立复审通过。cleanup23/23、checkIn38/38及集成通过。每候选完成后保存，未到期行集中保存；1200ms软预算不替代真实网络/函数预算验收，也增加逐候选状态事务成本。
- 已暂停录音系统中断：`2b46c3f`、报告`de02cc6`，独立复审通过。已确认暂停不等待第二次原生pause，不自动继续；pending阶段超时保护保留。协调器/交互/页面接线/tsc通过。
- 回听会话隔离：`7b5af68`、测试补强`656434d`，独立复审通过且无遗留问题。旧会话play/time/stop/end/error不能影响新会话；原生onPlay到来前不显示播放中进度。两页运行时、音频控制器、路由、接线和本地分享集成/tsc通过；补强后两份相关测试再次通过。
- 清理归属/引用：`5dc574f`，独立复审通过且无遗留问题。路径精确绑定owner/request/digest；任何额外引用或查询不确定均保留文件，预算耗尽不跨过未处理候选。cleanup31/31、checkIn38/38及本地分享集成通过，主控另重新执行cleanup31/31与checkIn38/38通过。真实ACL、跨文档管理员并发、平台耗时仍不是这些模拟测试的验收范围。
- 失效训练页恢复：`4749089`，独立复审通过。仅错误态以redirectTo移除错误训练页，再进入教材列表，避免返回后再次落入错误页；有效教材/原页返回路径不变。路由、接线、播放与tsc通过，主控fresh路由测试通过。
- 四处播放标记：`e3f4471`，独立复审通过。book3/84、book4/140、book4/164、book5/134按同名印刷标记改为精确百分比坐标；主控逐张复看新截图并重新运行映射校验测试通过，未播放音频，不声称语义校对完成。其余两处warning仍明确保留。
- 大字号标题：`aa11d83`，独立复审Spec compliant / Approved，无Critical或Important。首页教材/系列、录音列表章节/书名自然换行，不缩小字体或改动作；旧两行clamp契约同步更新。主控亲阅200%手机、正常iPad、实际滚底截图；滚底末项完整位于底栏上方且命中有效。复审保留两个Minor：大字按钮越出内容盒时尚未单独比较相邻文字Range，以及远程图片5秒加载会有环境性波动。交整体复审统筹，不静默丢弃。

## 集成检查中的发现与修复记录

- 限长读取初版 `9edf0c5` 的复审曾发现顶层签名 `status` 漏检；已在 `2e1fd64` 补齐，主控重新执行helper91/91与handler51/51通过，独立复审无遗留源码问题。保留该发现及修复过程，不把初版当作从未有缺陷。
- 本轮中间全量41份脚本实际为40/41（零重试），证据 `.superpowers/sdd/2026-09-17-cloud-repairs-regression.json`。唯一失败是返回导航测试要求旧的原生stop事件。已在 `854e1a6` 补“隐藏时恰好销毁一次、UI复位、卸载不重复创建/销毁”的精确断言，并保留全部原书原页/继续录音/零上传及独立分享入口断言；四项指定回归通过、独立复审通过，主控也再次运行返回测试通过。整套待全部UI修复后重新执行，不能将原失败轮次写成已全绿。

## 修复后主控独立验证（aa11d83）

| 检查 | 本轮实际结果 |
| --- | --- |
| 源码类型检查 | 固定Node，`tsc --noEmit --skipLibCheck`退出0；未跳过项目源码检查 |
| 微信fresh构建 | 仅该命令进程设置`ESBUILD_WORKER_THREADS=0`，退出0，Webpack报告6.65秒；有既有Browserslist/Sass/体积警告 |
| 新产物布局 | `audit-ui-layout.cjs full-audit-final-20260917`退出0，168/168，failed=[] |
| 200%首页实际滚底 | 主控再跑`audit-ui-layout.cjs home-bottom`退出0，1/1，末项未被底栏挡住且中心可命中 |
| dist只读体积 | 37文件，1,314,507字节；最大common.js 517,926字节、app-origin.wxss 354,568字节；不等于微信官方上传包验收 |
| 全量第一轮 | 40/41，返回导航脚本零输出30,008ms超时；同轮有构建并行，保存于`2026-09-17-full-audit-final-regression.json` |
| 超时脚本单独复核 | 相同源码/固定Node的返回导航脚本1.56秒退出0；不能因此宣称已确定超时根因 |
| 全量第二轮 | 40/41，返回脚本通过，录音协调器脚本零输出30,014ms超时；无并行构建但短时有布局检查，保存于`2026-09-17-full-audit-serial-regression.json` |
| 单次带阶段日志全量 | 40/41，前两失败脚本通过，新失败为test-ui-layout.cjs；最后日志到158.68ms的require-exit cssnano，之后30秒超时，保存于`2026-09-17-full-audit-observed-regression.json` |

三个全量失败轮次都未覆盖或隐去，没有增加测试超时阈值，也没有自动重试单项求绿。两脚本的定向阶段日志及有限启动/TS加载探针未复现，故只做了上表一次观察全量运行；现在按真实失败的最后阶段继续查样式处理卡点，**全量回归尚不能标记通过**。这些记录不能证明业务有缺陷，也不能证明一定只是环境问题。

本机构建默认worker模式两次停在Webpack0%；两份SCSS及esbuild基础sync/async调用单独成功，进程级关闭worker后实现者与主控各构建成功一次。此证据只支持该环境绕过有效，不证明Node24或某个底层锁是确定根因；未修改依赖、锁文件或项目配置。不带`--skipLibCheck`的类型检查仍有既有Taro/Node/微信等依赖声明诊断，未通过，不与上述源码检查混淆。

实现者的168场景复跑中还曾有一次远程图片5秒内未到达导致4个场景失败，后续同一新dist重跑成功；主控本次168/168为独立新一轮通过。不能把重跑成功写成网络从未波动。

## 本轮实际更新的云端内容

`checkIn/index.js` 已通过官方 `inc-deploy` 更新为已复审的 `166f3d1` 入口（1文件，压缩包7.1KB）。更新前再下载比对，确认用户没有插入其他变更；更新后重新下载3文件与本地完全一致。再次查询Active、3秒、Nodejs16.13。

| 制品 | SHA-256 |
| --- | --- |
| checkIn/index.js | `7528C980505DCD207A220DED60818F399D9B142138B3F8EF7803957F853D3F99` |
| checkIn/package.json | `3A123D40A3A0799C0D536D04805B962DFC1B1DAC98129D2A1EEE92AD83616C73` |
| checkIn/config.json | `CA3664CC370F406D81B3D260E643AC226B52D2322CA8DF6346B6476796BF6FC8` |
| 回读package-lock.json | `EE08BAC31C51ED8424E4EB301EC608F110DC35C7FD8F8F97BF4FE0839EA91FD4` |

回读实际依赖为wx-server-sdk4.0.2、@cloudbase/node-sdk3.17.2。没有重装依赖，没有修改运行配置、权限、费用、变量或触发器，没有调用真实学生录音业务。官方CLI自身有punycode弃用警告。

2026-09-17 21:48进一步更新checkIn为已复审 `2e1fd64` 限长版本：先单独上传新 `recordingDownload.js`（1文件/2.4KB），重新下载确认helper一致且旧入口/清单/配置三文件未变，再单独上传新入口（1文件/7.3KB）。最终回读4/4一致，新增文件的官方增量部署能力已实际验证；状态仍Active、3秒、Nodejs16.13，依赖版本未变。上表保留上一个已核对版本用于回退，**当前线上入口**如下：

| 当前checkIn制品 | SHA-256 |
| --- | --- |
| index.js | `DD484EA34B4401ACF0F3586E2BBF82EEBA7B2E435085975F7345B9B49566D782` |
| recordingDownload.js | `1F5BB3E4B74103AA2E691B6FFA072E23A5924DE7B20DC90728DDC34175B7742F` |

这只是已审源码的部署/一致性验收，未执行真实业务调用；Node16实际加载调用、真实签名/HTTP头格式、最大8MiB冷暖耗时及3秒余量仍须下述专用用例验证。没有因此宣称完整上线验收通过。

`cleanupExpiredShares/index.js` 已于2026-09-17 21:25通过官方单文件更新为reviewed `5dc574f`（1文件，压缩包3.7KB），包含错误脱敏、预算/检查点和归属/引用防护。部署前重新下载三个文件仍与初始线上版本一致；更新后重新下载index/package/config，3/3与复审本地版本一致，再次查询Active、3秒、Nodejs16.13。实际SDK仍为4.0.2 / 3.17.2，未重装。

| 清理函数制品 | SHA-256 |
| --- | --- |
| index.js | `38FA264331BD25E1840959849AFC9FC8F2DA0999084E46F78E121B9867606FE1` |
| package.json | `7E4D0D66DEB7E8701F6A662E02A0AC5CF2E78A1B9890A58BE6F8C7400EFA4723` |
| config.json | `087FEFDAAB93FF0F24D684F3704A93804B114290304F43FA02FB0E069A272003` |

此次未开启生产清理、未运行真实业务调用、未修改运行配置/权限/变量/触发器/费用；当前真实开关/触发器状态仍未读取，不能把“未执行开启操作”表述为“已核实关闭”。

## 代码快照与回退边界

本地证据目录 `.superpowers/sdd/cloud-goal-error-fix-20260916-1789573513/` 保存before/after完整下载包；after包含上述入口与实际依赖。

回退备份 `.superpowers/sdd/checkIn-safe-166f3d1-rollback.zip` 的SHA-256为 `417493EFE6815F2E31D056EDF7DCB11D645A585F68A6BAC0BF8BDA8258CF008C`。已直接读取压缩包并核对入口、package、lock、config和两个SDK清单共6项与回读包一致。该备份是本机文件，未声称异地备份完成。

若后续checkIn修改需要回退，应先核实这份制品和当时依赖/配置，再恢复**已审查入口**，只做官方单文件更新并重新下载核对；新helper可留为未引用文件，不为了回退删除数据。不能用任意旧master或重新开放create的版本。该入口的本地协议回归覆盖旧/新记录、owner/口令、墓碑与幂等，但仍存在本轮后续待修的下载上限/兼容策略，不能称为所有风险均已消除的版本。

清理异常时应由有权限的负责人关闭清理开关并暂停对应定时器，保留游标、预留、墓碑和本机录音；不能靠清库回滚，已经删除的云文件也不会因代码回退自动恢复。这里是应对步骤，不表示已经修改线上设置。

清理函数before/after完整快照位于 `.superpowers/sdd/cloud-goal-cleanup-ownership-20260917/`；已复审版本备份 `.superpowers/sdd/cleanup-reviewed-5dc574f-rollback.zip` SHA-256为 `E15F15373D4359DED538E1E2DF3BA1818B2023CD9BCBB38A2E6BC5207DFEC468`。直接读取ZIP，入口/package/config/两个SDK清单5项均与after一致，含实际依赖。仅为本机回退制品，不代表异地备份或数据恢复已完成。

限长版本快照 `.superpowers/sdd/cloud-goal-bounded-download-20260917/` 含before、helper-only和after三阶段。含依赖ZIP `.superpowers/sdd/checkIn-reviewed-2e1fd64-rollback.zip` SHA-256为 `A76B84C5A484887F65E88BF5CABE18425506239B565760A980C2B9B8FB37C619`；直接读取7项（入口/helper/package/lock/config/两SDK清单）均与after一致。若实际网络兼容异常，需要回退时先恢复166f3d1已审入口并回读，留下未引用helper即可；保留所有请求、期限、墓碑和本机文件。

## 正在实施或待验收

1. 九项有证据的源码修复均已通过分项独立复审；新构建及168布局通过，全量回归的无输出超时仍在定向定位，整体复审待完成。流式限长源码已部署，真实调用验收仍待执行。
2. 每账号每日新增分享额度及停止v1新增无期限分享：已请求业务选择，未擅自启用阈值或改变旧兼容。
3. 已请求将checkIn超时从3秒调整为20秒；等待用户操作后官方回读。流式读取会按可信平台context预算处理，不信客户端同名字段；3秒下最大文件体验尚未验收。
4. 云存储私有规则、owner限定上传/覆盖、checkins/shareCleanupState客户端禁读写、索引、当前实际用量、真实变量/定时来源和全页dry-run仍缺控制台证据。
5. 两个无印刷播放标记的热点需试听/教研核对：CASA第4册Unit 4绘本《Snake Colors》（book6/图片页88），Reading Explorer Level 4 Unit 12《THE THREAT FROM SPACE》（book24/图片页213，音频24）。不能机械改坐标；另四处已写回并重渲染检查通过。
6. iOS、Android、iPad真机：系统打断、快速回听切换、离页保存、5分钟、弱网、双账号口令/删除/过期、真实签名到期及最大录音校验；隐私告知、教材授权、账单告警仍需发布前核查。
7. 整体复审指出清理的慢单条边界：若引用查询和标记事务本身每次超过1200ms软预算，将可能在删除前退出、游标不变。当前安全规则不允许简单跨过未完成条目或超预算启动删除；正在用虚拟时钟补证，生产启用前需要实际时延范围、无进展告警与恢复流程，必要的预算/恢复设计调整须先明确。

平台context字段来源：[腾讯云SCF工作原理](https://cloud.tencent.com/document/product/583/9694)。可信运行上下文中的time_limit_in_ms为执行超时配置，不是每次数据库操作的可取消截止时间，也不能证明预留时间足够。

## 剩余线上验收的执行顺序

以下均是待办，不是已通过证据。只使用负责人创建的专用测试录音，保留真实学生数据。

1. **配置只读核验**：控制台确认环境、两函数变量/触发器、存储规则、数据库客户端权限与索引；截图隐藏口令、密钥和学生身份。记录容量/流量/调用数的已用值、总量和周期。CLI 的 Active 与下载包不能替代这些证据。
2. **代码与运行预算**：部署已复审源码后重新下载核对哈希；回读 checkIn 超时配置。对专用短录音和接近 8 MiB 的合法样本分别测冷/暖调用，记录总耗时和结果，不输出音频链接。超时、签名异常或传输中断不能激活分享，也不能清掉本地引用。
3. **两账号验收**：账号 A 完成本地录音，联网前确认可回听；主动分享后账号 B 使用分享链接访问这一条。移除/篡改凭证应拒绝访问，B 不能删除 A 的录音。测试账号重试分享应复用同代；新代不得使旧到期链接复活。
4. **清理只读预演**：核对可信入口来源和前缀后，由负责人在测试环境以只读模式逐页执行，保留每页 candidates / validated / failed / processed / nextCursor / budgetExhausted。预算退出应从已处理位置继续；失败目标不能因为游标前进而永久消失，后续全轮仍需重试。只有安全绑定且无额外引用的候选可计 validated。
5. **清理实删另行授权**：仅专用测试环境/测试录音确认到期失效、物理删除、失败重试、迟到上传及永久墓碑；生产开关仍不由本次代码部署自动启用。不能把预演通过当作物理删除验收，也不能用真实学生记录缩短期限来测试。
6. **原生设备验收**：iOS、Android、iPad 分别覆盖首次授权、示范音频与录音并行、手动暂停/继续、已暂停时系统中断、快速回听停止重播、切页/后台、5 分钟上限及保存后重启恢复。手机/Pad 横竖屏、大字号检查底部标签、导航房子图标、按钮点击区与标题换行。DOM 截图不证明麦克风、解码器或原生安全区正确。

证据至少记录：客户端版本/提交、云入口摘要、设备/系统/微信版本、测试时间、场景、实际结果与脱敏错误类别。未通过时保留本机文件和同一请求编号，不清缓存、不清库、不反复制造新分享来绕过问题。
